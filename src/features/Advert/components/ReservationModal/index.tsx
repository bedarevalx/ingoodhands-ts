import React from 'react';
import { classNamesParser } from '../../../../helpers/classNamesParser';
import Modal from '../../../../UI/Modal';

const ReservationModal = () => {
  return (
    <Modal open={true} classNames={['reservation-modal']}>
      index
    </Modal>
  );
};

export default ReservationModal;
